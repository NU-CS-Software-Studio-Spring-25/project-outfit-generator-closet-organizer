<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8">
    <title>Log in</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
  </head>

  <body>
    <main role="main" class="container my-5">
      <h1>Log in</h1>

      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <div class="field mb-3">
          <%= f.label :email, "Email", for: "#{resource_name}_email" %><br />
          <%= f.email_field :email, autofocus: true, autocomplete: "email", id: "#{resource_name}_email", class: "form-control" %>
        </div>

        <div class="field mb-3">
          <%= f.label :password, "Password", for: "#{resource_name}_password" %><br />
          <%= f.password_field :password, autocomplete: "current-password", id: "#{resource_name}_password", class: "form-control" %>
        </div>

        <% if devise_mapping.rememberable? %>
          <div class="field form-check mb-3">
            <%= f.check_box :remember_me, class: "form-check-input", id: "#{resource_name}_remember_me" %>
            <%= f.label :remember_me, "Remember me", class: "form-check-label", for: "#{resource_name}_remember_me" %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Log in", class: "btn btn-primary", style: "background-color: #3A7CA5; border: none;" %>
        </div>
      <% end %>

      <%= render "devise/shared/links" %>
    </main>
  </body>
</html>
